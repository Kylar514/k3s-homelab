apiVersion: v1
kind: Service
metadata:
  name: hindsight
  namespace: apps
spec:
  selector:
    app: hindsight
  ports:
    - name: api
      port: 8888
      targetPort: api
    - name: ui
      port: 9999
      targetPort: ui
