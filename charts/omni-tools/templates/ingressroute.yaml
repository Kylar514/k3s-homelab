apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: omni
  namespace: tools
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`omni.thenasus.com`)
      kind: Rule
      services:
        - name: omni
          port: 80
  tls:
    certResolver: letsencrypt
